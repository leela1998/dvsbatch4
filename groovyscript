pipeline{
agent any
parameters {
  choice choices: ['dev', 'prod'], description: 'select env', name: 'ENV'
}
stages{
stage('working with variables') {
steps{
script{
val1=20
println "myval1 is ${val1}"
println "myparameters value is ${params.ENV}"
}
}
}
}
}
